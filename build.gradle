apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'sonar-runner'
apply plugin: 'jacoco'

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.11'
}

sonarRunner {
  sonarProperties {
    property "sonar.projectName", "mowItNow"
  }
}
